-r requirements.txt

# Web UI (CI needs this explicitly; HF Spaces provides it via sdk_version)
gradio>=5.0.0

# Testing
pytest>=8.3,<9.0
pytest-cov>=6.0,<7.0
pytest-asyncio>=0.25,<1.0
httpx>=0.28,<1.0

# Linting
ruff>=0.9,<1.0

# Security
pip-audit>=2.8,<3.0
